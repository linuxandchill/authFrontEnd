export function signInUser({ email, password }){
  //submit email, pw to the server

  //if request is good:
  //1)update state to indicate user is auth'd
  //2)save the JWT 
  //3) redirect to auth-only route '/feature'

  //if request is bad:
  //- show error 
}
